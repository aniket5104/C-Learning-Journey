#include<stdio.h>
int main(){
    int max;
    printf("Enter the no. of elements in array")
    scanf("%d",&max);
    int arr[max];//the no. input in max will assign the no. of elements in the array
    return 0;
}